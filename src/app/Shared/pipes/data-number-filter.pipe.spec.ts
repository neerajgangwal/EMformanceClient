import { DataNumberFilterPipe } from './data-number-filter.pipe';

describe('DataNumberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataNumberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
